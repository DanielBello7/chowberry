export type PARTNER = {
    _id: string;
    refid: string; // same as with participants
    organization: string;
    project: string;
    merchant: string;
    isActive: boolean;
    createdAt: string;
    updatedAt: string;
};
